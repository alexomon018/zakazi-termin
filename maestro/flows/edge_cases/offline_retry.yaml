appId: ${MAESTRO_APP_ID}
tags:
  - regression
  - edge
---
# Use Maestro network conditioning when available for your platform/Maestro version.
- runFlow:
    file: ../_helpers/launch_app.yaml
- assertVisible:
    id: "auth.screen"
# Placeholder: enable airplane mode / network off via Maestro or OS-specific runScript.
- assertVisible:
    id: "auth.screen"
