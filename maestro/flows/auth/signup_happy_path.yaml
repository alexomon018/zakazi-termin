appId: ${MAESTRO_APP_ID}
tags:
  - regression
  - auth
---
# Mobile auth is OAuth-only; there is no in-app signup screen. Kept for manifest compatibility.
- runFlow:
    file: ../_helpers/launch_app.yaml
- assertVisible:
    text: "Salonko"
