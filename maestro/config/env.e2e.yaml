# Maestro defaults for local E2E (override via `maestro test -e KEY=value`).
# Replace APP_ID with your iOS bundle id / Android applicationId from apps/mobile.
defaults:
  MAESTRO_APP_ID: "com.salonko.mobile.e2e"
  E2E_USER_EMAIL: "e2e@example.com"
  E2E_USER_PASSWORD: "TestPassword123!"
