appId: ${MAESTRO_APP_ID}
tags:
  - regression
---
# Placeholder until OAuth E2E or dev bypass. Works whether session restores (tabs) or cold login.
- runFlow:
    file: ../_helpers/launch_app.yaml
- assertVisible:
    text: "Salonko"
