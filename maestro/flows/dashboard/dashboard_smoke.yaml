appId: ${MAESTRO_APP_ID}
tags:
  - regression
  - dashboard
---
# Authenticated tabs — requires OAuth session; run locally after login or when E2E auth exists.
- runFlow:
    file: ../_helpers/auth/login_with_test_user.yaml
