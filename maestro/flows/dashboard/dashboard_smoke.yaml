appId: ${MAESTRO_APP_ID}
tags:
  - smoke
  - regression
  - dashboard
---
# TODO: full dashboard assertions need OAuth session. When logged in, "Moje usluge" is the home tab.
- runFlow:
    file: ../_helpers/auth/assert_login_screen.yaml
- runFlow:
    when:
      visible: "Moje usluge"
    commands:
      - assertVisible: "Moje usluge"
