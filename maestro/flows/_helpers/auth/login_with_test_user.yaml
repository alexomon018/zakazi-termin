appId: ${MAESTRO_APP_ID}
---
# App uses OAuth (see apps/mobile/app/(auth)/login.tsx). Use login_smoke for UI checks only.
# Full session flows require OAuth automation or a dev E2E bypass — not implemented here.
- runFlow:
    file: ../launch_app.yaml
- assertVisible:
    text: "Salonko"
- assertVisible:
    text: "Prijavite se"
