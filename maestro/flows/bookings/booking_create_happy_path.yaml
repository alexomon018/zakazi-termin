appId: ${MAESTRO_APP_ID}
tags:
  - regression
  - bookings
---
# TODO: implement create booking after OAuth — mobile has no single "create booking" FAB on list; this flow only documents intent.
- runFlow:
    file: ../_helpers/auth/assert_login_screen.yaml
